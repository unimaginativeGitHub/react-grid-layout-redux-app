/**
 * Transform the provided format input schema to a series of grid
 * layout objects.
 * @param {Object} schema - format input schema to be transformed
 */
const inputSchemaParser = (schema) => {

};

/**
 * Transfrom the layout to a format input schema
 * @param {[Object]} gridLayout - The layout to be parsed to a schema
 */
const inputLayoutParser = (gridLayout) => {

}

// export {
//   inputSchemaParser,
//   outputParser,
// }