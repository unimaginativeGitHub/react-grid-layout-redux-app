import React from 'react';
import Color from '../util/colors';

const style = {
  divider: {
    flex: 0.01,
    color: Color.white,
  },
};

export default () => (
  <div style={style.divider} />
);
